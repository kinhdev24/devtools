# Theming

Support:

- Light mode
- Dark mode

Dark mode may be the default product identity.

## Rules

Use theme tokens.

Do not hardcode theme-specific colors.

Prefer semantic tokens:

background
foreground
muted
border
primary
destructive

Color communicates state, not decoration.

Prefer borders over shadows.
